<div class="posts">
  {{#each JobPosting}}
    <div class="card">
      <div class="card-header">
        <h2>
          <a href="/jobposting/{{id}}">{{title}}</a>
        </h2>
        <p>Created by
          {{User.username}}
          on
          {{format_date date_published}}
        </p>
      </div>
      <div class="card-body">
        <p>
          {{content}}
        </p>
      </div>
    </div>
  {{/each}}
</div>